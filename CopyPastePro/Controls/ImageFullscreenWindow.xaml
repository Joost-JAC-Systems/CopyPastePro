<Window x:Class="CopyPastePro.Controls.ImageFullscreenWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:CopyPastePro.Controls"
        Title="Image preview"
        Icon="pack://application:,,,/Assets/AppIcon.ico"
        WindowStyle="None"
        ResizeMode="CanResize"
        Background="{DynamicResource BgDarkBrush}"
        KeyDown="Window_KeyDown"
        ContextMenuOpening="Window_ContextMenuOpening">
    <Grid>
        <controls:ZoomableImagePreview x:Name="Viewer" ShowFullscreenButton="False"/>

        <Button HorizontalAlignment="Right" VerticalAlignment="Top" Margin="14"
                Width="40" Height="40" Padding="0" Cursor="Hand"
                ToolTip="Close (Esc)" Click="Close_Click" BorderThickness="0">
            <Button.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#CC1A1A2E" Offset="0"/>
                    <GradientStop Color="#994B3A8C" Offset="1"/>
                </LinearGradientBrush>
            </Button.Background>
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}" CornerRadius="10"
                            BorderBrush="#55FFFFFF" BorderThickness="1">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Button.Template>
            <TextBlock Text="✕" FontSize="16" Foreground="White" FontWeight="Bold"/>
        </Button>

        <Border VerticalAlignment="Bottom" HorizontalAlignment="Stretch" Padding="12,10"
                Background="#AA0D0D12">
            <TextBlock Text="Scroll to zoom · Ctrl+0 = 100% · Drag to pan · Esc to close · Ctrl+C to copy"
                       Foreground="{DynamicResource TextMutedBrush}" FontSize="11"
                       HorizontalAlignment="Center"/>
        </Border>
    </Grid>
</Window>
