<UserControl x:Class="CopyPastePro.Controls.FormattedClipboardPreview"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinHeight="240">
    <Grid>
        <ScrollViewer x:Name="HostScroll" VerticalScrollBarVisibility="Auto" Background="Transparent">
            <Grid>
                <TextBox x:Name="PlainText" IsReadOnly="True" TextWrapping="Wrap" AcceptsReturn="True"
                         Visibility="Collapsed" VerticalScrollBarVisibility="Disabled" BorderThickness="0"
                         Background="Transparent" Foreground="{DynamicResource TextPrimaryBrush}"/>
                <RichTextBox x:Name="RtfViewer" IsReadOnly="True" Visibility="Collapsed" BorderThickness="0"
                             VerticalScrollBarVisibility="Disabled" Background="Transparent"/>
                <RichTextBox x:Name="CodeViewer" IsReadOnly="True" Visibility="Collapsed" BorderThickness="0"
                             VerticalScrollBarVisibility="Disabled"/>
                <WebBrowser x:Name="HtmlBrowser" Visibility="Collapsed"/>
                <DataGrid x:Name="ExcelGrid" Visibility="Collapsed" IsReadOnly="True" AutoGenerateColumns="True"
                          HeadersVisibility="Column" CanUserAddRows="False" CanUserDeleteRows="False"
                          GridLinesVisibility="All" Background="Transparent"/>
                <Image x:Name="PdfImage" Visibility="Collapsed" Stretch="Uniform" MaxHeight="600"/>
            </Grid>
        </ScrollViewer>

        <Border x:Name="OverlayBar" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="8"
                CornerRadius="20" Padding="4" Opacity="0"
                Background="#AA1E1E28" BorderBrush="#55FFFFFF" BorderThickness="1">
            <Border.RenderTransform>
                <TranslateTransform x:Name="OverlayTransform" Y="-4"/>
            </Border.RenderTransform>
            <StackPanel x:Name="OverlayButtons" Orientation="Horizontal"/>
        </Border>
    </Grid>
</UserControl>
