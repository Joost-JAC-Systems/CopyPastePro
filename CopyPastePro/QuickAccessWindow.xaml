<Window x:Class="CopyPastePro.QuickAccessWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CopyPaste"
        Width="300" Height="560"
        MinWidth="260" MaxWidth="360"
        MinHeight="400" MaxHeight="800"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        ShowActivated="True"
        Topmost="True"
        ResizeMode="CanResizeWithGrip"
        KeyDown="Window_KeyDown"
        Deactivated="Window_Deactivated">
    <Border Background="{DynamicResource BgPanelBrush}" CornerRadius="14" BorderThickness="0" Margin="8">
        <Border.Effect>
            <DropShadowEffect BlurRadius="18" ShadowDepth="2" Opacity="0.45"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="{DynamicResource StickyHeaderBrush}" CornerRadius="14,14,0,0" Padding="12,10" BorderThickness="0" MouseLeftButtonDown="Header_MouseDown">
                <Grid>
                    <StackPanel Orientation="Horizontal">
                        <Image Source="{DynamicResource AppIconTransparent}" Width="28" Height="28" Margin="0,0,10,0"
                               RenderOptions.BitmapScalingMode="HighQuality" VerticalAlignment="Center"/>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="CopyPaste Pro" Foreground="White" FontWeight="Bold" FontSize="14"/>
                            <TextBlock x:Name="QuickSubtitle" Text="Ctrl+Shift+V" Foreground="#E0DCFF" FontSize="10" Margin="0,2,0,0"/>
                        </StackPanel>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="⚙" ToolTip="Open full manager" Style="{StaticResource GhostButton}" Foreground="White" Click="OpenManager_Click" Padding="6,2" Margin="0,0,4,0"/>
                        <Button Content="✕" ToolTip="Close" Style="{StaticResource GhostButton}" Foreground="White" Click="Close_Click" Padding="6,2"/>
                    </StackPanel>
                </Grid>
            </Border>

            <TextBox x:Name="QuickSearch" Grid.Row="1" Margin="10,8,10,6" Tag="Search…" PreviewKeyDown="QuickSearch_PreviewKeyDown"/>

            <ListBox x:Name="QuickList" Grid.Row="2" Margin="6,0,6,6" BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     ScrollViewer.CanContentScroll="False"
                     VirtualizingPanel.IsVirtualizing="True"
                     VirtualizingPanel.VirtualizationMode="Recycling"
                     PreviewMouseLeftButtonUp="QuickList_PreviewMouseLeftButtonUp"
                     PreviewMouseRightButtonDown="QuickList_PreviewMouseRightButtonDown"
                     ContextMenuOpening="QuickList_ContextMenuOpening"
                     PreviewMouseWheel="QuickList_PreviewMouseWheel">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem" BasedOn="{StaticResource {x:Type ListBoxItem}}">
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Margin" Value="0,0,0,4"/>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border x:Name="Bd" Background="Transparent" CornerRadius="8" Padding="4,2">
                                        <ContentPresenter/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource BgCardHoverBrush}"/>
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource BgCardBrush}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="2,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="4,0,4,0">
                                <TextBlock Text="{Binding Title}" TextTrimming="CharacterEllipsis" FontWeight="SemiBold" FontSize="12"
                                           Foreground="{DynamicResource TextPrimaryBrush}"/>
                                <TextBlock Text="{Binding Subtitle}" Foreground="{DynamicResource TextMutedBrush}" FontSize="10" TextTrimming="CharacterEllipsis"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                <Button Content="★" ToolTip="Favorite" Tag="{Binding}" Click="Favorite_Click"
                                        Style="{StaticResource GhostButton}" Padding="4,2" FontSize="11" MinWidth="24"
                                        Foreground="{Binding FavoriteBrush}"/>
                                <Button Content="📌" ToolTip="Pin" Tag="{Binding}" Click="Pin_Click"
                                        Style="{StaticResource GhostButton}" Padding="4,2" FontSize="11" MinWidth="24"
                                        Foreground="{Binding PinBrush}"/>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <Border Grid.Row="3" Padding="10,8" BorderThickness="0" Background="Transparent"
                    PreviewMouseRightButtonDown="QuickFooter_PreviewMouseRightButtonDown">
                <Grid>
                    <TextBlock x:Name="QuickFooter" Foreground="{DynamicResource TextMutedBrush}" FontSize="10" VerticalAlignment="Center"/>
                    <Button Content="Full manager →" HorizontalAlignment="Right" Style="{StaticResource GhostButton}" Click="OpenManager_Click" Padding="8,4" FontSize="11"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
